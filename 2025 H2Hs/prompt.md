Write python code in "H2H-Scaper.ipynb" to complete the task defined in "task.md", using functions given in "main.py". Avoid changing the function in "main.py" where possible and only add code to H2H-Scaper.ipynb" unless the code in "main.py" is not functional.

# Additional Task
the 'Ranking Points' sheet of '2025 H2H Shoots.xlsx' contains how many national ranking points an archer is awarded for achieving a given rank, and it depends on the rank achieved, not the exact score shot. For each of the event sheets in '2025 H2H Shoots.xlsx' add an additional column called 'Ranking Points' and use the tier of the event (from 'Shoots' sheet) to look up how many points each archer got.

After this, add a toggle to the plot to plot either score vs rank (as at present) or score vs points, with the points axis reversed so that it is decreasing to the right.